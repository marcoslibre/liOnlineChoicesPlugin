# config/services.yml
dev:
  parameters:

all:
  parameters:
    
  services:
      actions_service:
          class: ApiActionsService
      oauth_service:
          class: ApiOAuthService
      customers_service:
          class: ApiCustomersService
          arguments: ["@oauth_service"]
          
